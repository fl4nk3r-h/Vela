plugins {
    id 'base'
}

allprojects {
    group = 'dev.vela'
    version = '0.1.0'

    repositories {
        mavenCentral()
    }
}

subprojects {
    tasks.withType(Test).configureEach {
        useJUnitPlatform()
    }
}